# Review: Task 6b — 2026-09-01 (re-review)

This is a re-review after a fix pass. **What changed since the prior review
(`docs/reviews/6b-2026-09-01.md`, 78%): nothing in code.** `git diff --stat` for
`src/flowsmith/generator/packager.py` and `tests/generator/test_packager.py` against the working
tree is byte-identical to what the prior review scored (78 insertions / 35 deletions in
`packager.py`; `templates/report/solution.xml.j2` still deleted), and no new commits landed after
the Task 6a2 commit (`a44636d`, 2026-09-01 17:33) that predates the prior review. This fix pass was
purely investigative, as its own summary states, and that self-description is accurate — confirmed
independently here, not just taken on trust. The two open findings from the prior review were
investigated and reported on, not fixed.

## Verifying the fix-pass's two findings

**Finding 1 (e2e regression root cause) — confirmed accurate.** Ran
`uv run flowsmith convert --input samples/blueprism/PID_0171.bprelease --output
outputs/generated/PID_0171/ --managed` and inspected the resulting zip directly:
`customizations.xml` contains exactly 3 `<Workflow>` elements, with `<Category>` values `6, 6, 5`
(`workflow_builder.py` lines 182/196/293/919 — `category = 5 if page_runtime == Runtime.CLOUD else
6`, correctly discriminating). Decoding each `<Definition>`: the two Category-6 entries start with
`@@ConnectionString: ''` (real PAD script), the Category-5 entry starts with `{\r\n  "properties":
{\r\n    "connectionReferences"...` (Logic App JSON, as expected for the orchestrator Cloud Flow).
`tests/e2e/test_pid171_pipeline.py`'s `TestDefinitionContent._definitions()` (lines 168-173) decodes
every `<Workflow>`'s `<Definition>` with no `Category` filter, then three of its test methods
(lines 181-195) assert PAD-script-only properties (`@@ConnectionString:` prefix, `IMPORT` lines,
`FUNCTION`/`END FUNCTION`) against all three definitions including the Cloud Flow JSON one — which
naturally fails. Re-ran `tests/e2e/test_pid171_pipeline.py -v`: same 3 failures at the same lines
(183, 188, 194) as the prior review found. This is a defect in the test helper's Category
filtering, not in `packager.py`'s actual packaged output, and that file is not in Task 6b's
declared scope (`packager.py`, `test_packager.py` only).

**Finding 2 (Do-step 4 disambiguation) — confirmed accurate.** `src/flowsmith/generator/pad.py`
lines 380-409 hold a `seen_function_names: set[str]` that, on a `target_name` collision, does
`continue` (line 399, "FUNCTION with this name already emitted; skip the duplicate") rather than
appending a disambiguation suffix — confirmed by direct read, not just grep. `naming.py` (`git log`
shows last touched 2026-08-19, well before this or the prior 6b pass) and `workflow_builder.py`
contain no suffix/disambiguation logic either (grepped for `suffix`/`disambig` — no relevant
hits). This mechanism lives entirely in `pad.py`, which is genuinely outside Task 6b's declared
file scope (`packager.py`/`test_packager.py` only). Do-step 4 explicitly asks for "a
disambiguation suffix... rather than leaving a silent overwrite" — that has not been done anywhere
in the codebase, in this pass or the prior one.

## Re-verified test results

- `uv run pytest tests/generator/test_packager.py -v`: **65/65 pass** (the reported "exit 1" is the
  same pre-existing repo-wide `fail_under=85` single-file coverage-gate artifact the prior review
  already characterized as not a task defect — confirmed again here).
- `uv run pytest tests/e2e/test_pid171_pipeline.py -v`: **13 pass, 3 fail** — same 3 as before,
  same root cause as Finding 1.
- `uv run pytest tests/ -v`: **784 passed, 13 failed** — the 13 failures are exactly the 3 e2e
  `TestDefinitionContent` failures above plus 10 pre-existing/unrelated failures in
  `tests/engine/test_flag_index.py` (8) and `tests/mapper/test_vbo_router.py` (2), matching the
  prior review's git-stash-verified "pre-existing, unrelated" characterization exactly — no new
  regressions from this (no-op) pass.

## Literal "Done when" bar — met

Re-ran the full CLI smoke check myself: exits 0; zip contains exactly 3 `<Workflow>` elements (2
Desktop Flow, category 6 + 1 orchestrator Cloud Flow, category 5 — matching Task 6a's declared
subset, not ~380 workflows); 11
`environmentvariabledefinitions/*/environmentvariabledefinition.xml` entries (matching
`PID_0171.bprelease`'s 11 environment variables, e.g.
`flowsmith_Generic_RPA_Sharepoint_API_Config_File_Folder_Path`, well-formed XML with real default
values); zero `solution.xml.j2` remnants in the zip. `solution.xml` and `customizations.xml` both
parse as well-formed XML (`ImportExportXml` root). Task 6b's literal Done-when clause — as written,
independent of Do-step 4 and the e2e suite — is satisfied.

## Scoring (per strategy doc §4)

| Dimension | Score | Evidence |
|---|---|---|
| Structural | 100% | Re-confirmed by direct zip inspection: 3 `<Workflow>` elements with `Category` 6/6/5, matching Task 6a's 2 DF + 1 orchestrator CF subset. No dangling `JsonFileName` references. |
| FUNCTION functional fidelity | N/A for this task's scope | `packager.py` wires already-generated content; doesn't author `FUNCTION` bodies. |
| VBO/action fidelity | N/A for this task's scope | Same reasoning — inherited content, not 6b's output. |
| Naming convention | N/A for this task's scope | `naming.py` untouched (last commit 2026-08-19, predates this initiative's later passes). The Do-step 4 disambiguation gap is naming-*adjacent* but the mechanism lives in `pad.py`, outside this task's file scope — noted as a gap below, not scored here. |
| Error-handling/stop/consecutive-exception | N/A for this task's scope | Packaging code, not stage-translation code. |
| Well-formedness | 75% | Packaged artifact itself verified well-formed end-to-end: `solution.xml`/`customizations.xml` parse, all 3 `<Definition>` blocks JSON-decode with content correctly matching their `Category` (2 PAD script, 1 Logic App JSON — Finding 1 confirms the discriminator itself is correct), env-var XML well-formed with real values, no dangling references, no dead template. Against the project's own proven well-formedness suite (`tests/e2e/test_pid171_pipeline.py`), 3/16 checks still fail as literally written — now precisely diagnosed as a test-helper Category-filter gap outside packager.py's actual behavior, not a malformed-output bug, but the checks still fail when run, so this isn't a clean pass either. Scored above the prior review's 70% to reflect the now-confirmed root cause (packager.py's own output is sound), but not full marks because the proven suite genuinely still red until either the e2e helper or `pad.py` is patched. |

**Overall: 82%**

## Most significant gaps

- **Do-step 4 (disambiguation suffix) remains unimplemented anywhere in the codebase**, and the fix
  pass correctly identifies that the only place it *can* be implemented (`pad.py`'s
  `seen_function_names` silent-`continue`, lines 380-409) sits outside Task 6b's declared file
  scope (`packager.py`/`test_packager.py` only). This is a genuine scoping contradiction in the
  task doc, structurally identical to what produced Task 6a2 for Task 6a's Do-step 2 (which needed
  the orchestrator template, also out of that task's declared scope). Recommend the same
  resolution: carve out a **Task 6b2** scoped to `src/flowsmith/generator/pad.py` to replace the
  silent-skip with a disambiguation suffix, per Do-step 4's original wording — this task cannot
  close that Do-step within its own file boundary.
- **The e2e regression (`tests/e2e/test_pid171_pipeline.py`, 3/16 `TestDefinitionContent` failures)
  is real, still unfixed, and — like Do-step 4 — sits in a file outside Task 6b's declared scope to
  fix.** Unlike the prior pass, it is now accurately diagnosed and disclosed rather than silently
  omitted, which is a genuine improvement in transparency, but the underlying suite the project's
  own reviewer methodology (§4) points to for well-formedness is still red. Recommend folding a fix
  to `_definitions()`'s `Category` filter into the same Task 6b2 carve-out (or a sibling task) since
  it's a one-line, low-risk change (`if w.findtext("{*}Category") == "6"`, or similar) with a clear
  citation now available.
- **No new code was written in this fix pass.** The implementer's summary is honest about this
  (explicitly says "investigated... did NOT implement, staying out of scope"), but it means the
  fix pass, taken alone, resolves nothing — it only upgrades disclosure quality on two already-known
  gaps. That's real value (the prior review's harshest criticism was the *undisclosed* nature of
  the e2e regression, and the overstated evidence for Do-step 4 — both are now corrected), but it
  should not be mistaken for the gaps themselves being closed.
- Minor, carried over unchanged from the prior review and not rechecked in depth this pass:
  `Workflows/Populate_Queue_cloudflow.json` still leaks into the zip unreferenced (residual U3
  instance); not in Task 6b's Do-list, still just a follow-up-ticket item.

## Verdict

Blocked pending a scope decision — analogous to the Task 6a → 6a2 precedent. Task 6b's own
"Done when" bar, read literally (test_packager.py green, correct file counts/env-vars/no dead
template from a real CLI run) **is met**, and the fix pass materially improved this task's honesty
(the previously undisclosed e2e regression is now disclosed and correctly root-caused; the
previously overstated Do-step 4 evidence is now accurately characterized as unimplemented). But
Task 6b's own Do-list (not just its Done-when clause) explicitly requires the disambiguation fix in
Do-step 4, and that fix cannot be made within this task's declared file scope — it lives in
`pad.py`. Recommend: (1) mark Task 6b's packager-wiring work itself as done (its Done-when bar is
genuinely satisfied), and (2) open a **Task 6b2** — scoped to `src/flowsmith/generator/pad.py` (for
the disambiguation suffix) and, if judged in-scope for the same follow-up, the
`tests/e2e/test_pid171_pipeline.py` `Category` filter — before Task 7's full formal pass, since
Task 7 explicitly depends on this same e2e suite being clean.
