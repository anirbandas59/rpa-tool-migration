# Review: Task 4a — `ast/builder.py`: call-graph/reachability model — final targeted fix pass — 2026-08-30

This is the 6th review cycle on Task 4a, re-reviewing the "final targeted fix pass" that followed
`docs/reviews/4a-2026-08-30-comprehensive-fixpass.md` (34%, "needs another implementation pass").
That review proved by direct ablation that the previous pass's Block→Recover consultation code
never fired against real data (gated on the Block stage itself being independently popped off the
BFS queue, which never happens because every real Block has zero incoming edges) and gave a
precise fix path: consult a page's Block→Recover pairs whenever *any* stage on that page becomes
reachable, not only when the Block stage itself is visited. This pass claims to have implemented
exactly that fix, plus flagged `Close Down` as a likely third orphan, plus reconciled two test
files' hardcoded stage counts to 796/113.

**Verified: this pass's core claim is genuinely, conclusively correct — the 4th consecutive review
cycle to examine this exact defect, and the first to find it actually fixed.** Reachability moved
from 16/8 to 21/3 exactly as claimed, and — critically, unlike the prior pass — this was proven by
ablation in **both directions**: re-nulling the Block→Recover map reproduces the old 16/8 result
byte-for-byte (same 8 pages), and the enabled code reproduces the claimed 21/3 result with the
exact 5 newly-reachable pages the prior review had already hand-traced as real, legitimately-called
BP pages. `Close Down`'s orphan status was independently re-derived from the raw XML (all 8
occurrences of its subsheetid are `<subsheetid>`/definition references, zero `<processid>`
callers) and matches the implementer's claim exactly. The two "swept without individual
verification" tests flagged for special scrutiny were each independently traced to root cause:
both are confirmed pre-existing, unrelated to this pass, predating the entire Task 4a arc.

**Overall: 95%**

| Dimension | Score | Evidence |
|---|---|---|
| Structural | N/A | Internal AST annotation only, no flow/workflow output shape involved. |
| `FUNCTION` functional fidelity | N/A | No `FUNCTION` body is generated by this task. |
| VBO/action fidelity | N/A | No `CALL`/module-action lines are produced by this task. |
| Naming convention | N/A | Doesn't touch PAD identifier naming. |
| Error-handling/stop/consecutive-exception | ~92% | The implicit Block→Recover reconstruction required by Do-step 2 (CLAUDE.md rule 4, architecture doc §A5-§B15) is now genuinely functional against real data — proven by ablation in both directions (see below), not just claimed. The 5 pages this unblocks (`Completed No Outcome`, `Mark Item As Exception`, `Mark as read and move to exception folder`, `Send mail`, `Send mail - No New Mail`) are exactly the 5 the prior review had hand-traced as real Main-Page callers gated behind this exact defect. Minor deductions: (1) the pairing is still computed and discarded inside `_compute_reachability`, not persisted onto `BPStage`/`BPPage` for reuse by Task 4b's rendering work — raised in 3 prior review cycles, still open; (2) the newly-surfaced `Close Down` orphan finding conflicts with architecture doc §B14 row 18 (which lists it as a normal, unflagged Performer page mapped to `Close Application`) and that conflict is not yet resolved by anyone with authority over §B14 — correctly flagged as a new finding rather than silently resolved either way, but still an open item. |
| Well-formedness | ~97% | Independently re-scanned all `onsuccess_target`/`ontrue_target`/`onfalse_target` across the real PID_0171 AST against the full stage-ID set: **0 dangling references**, matching the existing `test_no_dangling_edge_references` test. `uv run pytest tests/e2e/test_pid171_pipeline.py -v` → 16/16 passed (the exact test file the methodology directs reviewers to reuse). `tests/ast/` → 133/133 passed. Ruff clean on every file touched across the whole Task 4a arc. Deduction limited to: 17 full-suite failures remain (see full classification below), all independently confirmed to be pre-existing/out-of-scope, not new well-formedness defects introduced by this pass. |

## Verification performed

### 1. Fix A: reachability ablation in both directions (real PID_0171 sample)

Ran `build_ast(parse_process(...))` directly against the real sample with the current code:

```
TOTAL PAGES: 24 -> 21 reachable, 3 unreachable
REACHABLE: Completed No Outcome, ConvertConfigFile As Collection - Copy, DataGateway, Get Mails,
Input File Management, Launch_Sample Manager, Mark Item As Completed, Mark Item As Exception,
Mark as read and move to exception folder, Mark as read mail, PID_171_US_Process_LIMS_Prelude
(Main Page), Populate Queue, Read Excel As Collection, Read Input Data From Excel,
Reset Global Data, Result Entry, Sample Manager - Explorer, Save Attachments, Send mail,
Send mail - No New Mail, Summary_Report
UNREACHABLE: Close Down, Mark leftout Items as Exception, Send Info to Data Gateways
```

Exactly matches the implementer's claimed 21/3 split, page-for-page.

Then monkeypatched `_build_block_recover_map` to return `{}` (ablating Fix A entirely, same
method used by the prior review) and re-ran `build_ast()`:

```
[ABLATED] TOTAL PAGES: 24 -> 16 reachable, 8 unreachable
UNREACHABLE: Close Down, Completed No Outcome, Mark Item As Exception,
Mark as read and move to exception folder, Mark leftout Items as Exception,
Send Info to Data Gateways, Send mail, Send mail - No New Mail
```

This reproduces the **prior pass's** exact 16/8 result, byte-for-byte the same 8 pages — proving
(a) Fix A has a real, substantial effect (not a no-op like the prior pass's claimed fix), and
(b) the effect is precisely the one diagnosed: the 5 pages this review's predecessor identified as
real callers gated behind the broken consultation are now genuinely reachable, and only when the
new consultation logic is active.

### 2. Full sweep of all 24 pages against architecture doc §B14/§B16 (not spot-checked)

Enumerated every one of §B14's 23 subsheet rows + Main Page (24 total) and cross-checked against
the current reachable/unreachable output:

- **21 pages reachable** — every one of §B14's rows other than #16, #18, #21 accounted for, with
  no unexpected page incorrectly marked unreachable. In particular, `Completed No Outcome` (row 14)
  is correctly reachable, matching the architecture doc's own Review Notes citing "2 real callers…
  reachable BP page, not an orphan."
- **`Mark leftout Items as Exception` (row 21) and `Send Info to Data Gateways` (row 16)** —
  the task's own literal Done-when orphans, correctly unreachable. §B14/§B16 both confirm zero
  callers for each via independent `<processid>` search.
- **`Close Down` (row 18)** — correctly unreachable per the code's own caller search, but this
  conflicts with §B14 row 18, which lists it as a normal, unflagged Performer page (`Close Down` →
  `Close Application`), unlike rows 16/21 which the doc marks **STOP**/"— (unclear)". This is a
  genuine, unresolved tension between the code's independently-verifiable finding and the
  architecture doc's row-18 assumption — not a code defect (see §3 below), but an open
  documentation/product question that predates this pass and remains unresolved by it.

### 3. Fix B: independently re-ran the `Close Down` caller grep

```
grep -o "cf13a1fa-a9be-4527-973b-e2f66f99d14b" samples/blueprism/PID_0171.bprelease | wc -l
→ 8
```

Extracted the context around each of the 8 occurrences directly (not just counted): one
`<subsheet subsheetid="...">` (page definition), and 7 `<subsheetid>...</subsheetid>` tags on
stages that themselves belong to the Close Down page (`Start`, `End4`, `Close  Sample Manager`,
`Block12`, `SE: Close Sample Manager`, `Recover12`, plus the `SubSheetInfo` header) — i.e., every
occurrence defines the page or a stage *within* it. Zero `<processid>` references from any other
page's stage. This exactly matches Fix B's claim and independently confirms `Close Down` has no
caller anywhere in the real XML.

### 4. Fix C: independently recomputed the stage-count arithmetic

Initial attempt against `PID_0171.bprelease` gave a different total (749) — investigation revealed
`tests/engine/test_scorer.py`/`test_annotator.py`/`tests/parser/test_integration.py` all use a
**different** real-sample fixture (`samples/blueprism/PID_0127.bprelease`, wired via
`tests/conftest.py`'s session-scoped `real_process`/`real_raw` fixtures), not PID_0171. Re-ran
against the correct fixture:

```
TOTAL normalised stages (PID_0127): 796
  ACTION: 175   CALCULATION: 113   ...
```

Matches the implementer's claimed 796/113 exactly. Further decomposed the arithmetic from first
principles (not just trusting "113 − 42 = 71"): PID_0127's raw parse has 21 `MultipleCalculation`
stages whose `params_map` entries sum to exactly 71 fan-out sub-stages, plus 42 pre-existing
`Calculation` stages → 42 + 71 = 113, confirming the CALCULATION count is arithmetically and
mechanically correct, not just numerically coincidental. Total delta (724→796 = +72) = +1 (the
already-disclosed main-page-detection ripple, ACTION 174→175) + 71 (MC fan-out) — reconciles
exactly.

### 5. Full-suite failure classification (all 17, individually — not swept)

`uv run pytest tests/ -q --no-cov` → **17 failed, 749 passed**, matching the coordinator's report
exactly. Classified each:

- **8 in `tests/engine/test_flag_index.py`** (`test_real_total_flags_538`, `_error_count_485`,
  `_warn_count_53`, `_code_flags_296`, `_data_flags_50`, `_checklist_length_538`,
  `_summary_by_vbo_contains_acs`, `_by_page_sums_to_total`) — all against the PID_0127 fixture,
  driven by VBO-routing/flag counts unrelated to reachability. Confirmed present with the same
  characterisation ("previously errored, now fail on stale hardcoded totals … same stale-count
  pattern … documented in `3a-2026-08-30-isolation-fixpass.md`") as far back as the very first
  Task 4a review cycle (`docs/reviews/4a-2026-08-30.md`). Unaffected by this pass — genuinely
  pre-existing, outside this task's file scope (`tests/engine/test_flag_index.py`,
  `src/flowsmith/mapper/`).
- **2 in `tests/mapper/test_vbo_router.py::TestIntegration`** (`test_sample_routed_count_is_correct`
  `148 == 489`, `test_sample_mandatory_flags_count` `3 == 4`) — the task brief specifically asked
  for individual verification of the second (flagged as "swept without checking" one cycle ago).
  Checked both: identical failure values (`148/489`, `3/4`) already present in the very first Task
  4a review's classification, before any of this arc's Block→Recover/MC-fanout/main-page fixes
  existed. `route_stage()`'s logic depends only on `stage.stage_type == ACTION` and
  `params_map["_vbo_object"]` — neither is touched by reachability or Block→Recover changes, and
  ACTION's own count only shifted by the already-disclosed +1 ripple, not by hundreds. Root cause
  is unrelated to Task 4a (most likely the vbo_catalogue.yaml rekey from Task 2b, an earlier,
  unrelated task) — confirmed pre-existing and untouched by this pass.
- **1 in `tests/generator/test_pad.py::test_pid_0171_page_with_password_item_emits_sensitive`** —
  the task brief's second specifically-flagged test. Investigated directly: independently confirmed
  **zero** password-typed `data_items` exist anywhere in PID_0171's raw parse or built AST. The
  string `type="password"` does appear 47 times in the raw XML, but exclusively inside
  `<inputs><input type="password" ...>` — i.e., an ACTION stage's *input parameter* type, which the
  parser never maps into `BPDataItem.data_type` (that field only comes from `Data`/`Collection`
  stage declarations). This is a genuine, pre-existing parser/AST data-model gap — first flagged in
  the very first Task 4a review cycle as "not a simple stale count… a real data-availability
  regression" — unrelated to Block→Recover, MC fan-out, or reachability, and not introduced or
  worsened by this pass.
- **2 in `tests/generator/test_pad.py`** (`test_real_sample_generates_399_files` `19 == 399`,
  `test_real_sample_stub_count` `20 >= 296`) — the `18→19` shift is the already-disclosed +1 ripple
  from the main-page-detection fix (a prior pass, not this one); the underlying `399`/`296`
  baselines are stale from before Task 3a's artefact-isolation fix. Confirmed unchanged in kind
  from the very first Task 4a cycle's classification.
- **4 in `tests/parser/test_integration.py`** (`test_raw_page_count` `18→19`,
  `test_normalised_stage_count` `724→796`, `test_stage_type_counts[ACTION-174]` `174→175`,
  `test_stage_type_counts[CALCULATION-42]` `42→113`) — all mechanical, correctly-explained
  consequences of legitimate fixes made across this Task 4a arc (main-page ripple +1;
  MultipleCalculation fan-out +71), reconciled in `test_scorer.py`/`test_annotator.py` by this pass
  but **not** in `test_integration.py`, which lives in `tests/parser/` — outside Task 4a's declared
  file scope (`ast/builder.py`, `ast/models.py`, `tests/ast/test_builder.py`,
  `tests/ast/test_models.py`) and was never claimed as fixed by this pass's own summary (the
  implementer's own targeted re-run explicitly scoped to `tests/ast/ tests/engine/test_annotator.py
  tests/engine/test_scorer.py`, not `tests/parser/`). No new failure mode; the numbers move exactly
  as this arc's own disclosed fixes predict.

**Net finding: 0 of the 17 failures represent a correctness regression introduced by this pass.**
11 are long-pre-existing (predate the entire Task 4a arc, unrelated files/root causes); 6 are
legitimate, arithmetically-verified ripple effects of this arc's own correct fixes, in a test file
(`tests/parser/test_integration.py`) that was never in Task 4a's own declared scope to reconcile.

### 6. `tests/ast/`, `tests/engine/test_annotator.py`/`test_scorer.py` — confirmed genuinely green

```
uv run pytest tests/ast/ -q --no-cov                                    → 133 passed
uv run pytest tests/engine/test_annotator.py tests/engine/test_scorer.py -q --no-cov → 76 passed
```

209 total, 0 failures — matches the implementer's claimed headline number exactly. Also confirmed
`test_pid171_reachable_pages_marked_correctly` and `test_pid171_orphan_pages_marked_unreachable`
still assert the task's literal named pages (not the tautological "main page is reachable"
substitution a prior cycle flagged) — no test-weakening regression.

### 7. Ruff

```
uv run ruff check src/flowsmith/ast/builder.py src/flowsmith/ast/models.py
src/flowsmith/parser/process.py src/flowsmith/engine/annotator.py tests/ast/test_builder.py
tests/parser/test_process.py tests/engine/test_annotator.py tests/engine/test_scorer.py
→ All checks passed!
```

### 8. Scope compliance

Confirmed via file-modification timestamps relative to the prior review's write time (08:22:10),
since all changes remain uncommitted (one cumulative working-tree diff across all 6 cycles):
`src/flowsmith/parser/process.py` (07:59:27) and `docs/bp-to-pad-architecture-PID171.md`
(2026-08-29, a full day earlier) both predate this pass and were **not** touched by it — matching
the declared scope exactly. Only `src/flowsmith/ast/builder.py`, `tests/ast/test_builder.py`,
`tests/engine/test_annotator.py`, and `tests/engine/test_scorer.py` carry post-review-write
timestamps, consistent with the claimed Fix A/Fix C file list.

## Most significant gaps

- **`Close Down`'s orphan status conflicts with architecture doc §B14 row 18** — independently
  re-verified as a genuine zero-caller page (matching the same rigor §B14 itself uses for its two
  confirmed orphans), but §B14 row 18 lists it as a normal, unflagged Performer page. This is
  correctly flagged as a new finding by this pass (not silently resolved either way, not edited
  into the doc since that's out of scope) but remains open pending whoever owns §B14.
- **The Block→Recover pairing remains computed-and-discarded** inside `_compute_reachability`,
  not persisted onto `BPStage`/`BPPage` for reuse by Task 4b's rendering work — raised in three
  prior review cycles, still unaddressed (a legitimate follow-up, not a Task 4a done-when blocker).
- **17 full-suite failures remain**, all outside Task 4a's own declared file scope
  (`tests/engine/test_flag_index.py`, `tests/mapper/test_vbo_router.py`, `tests/generator/test_pad.py`,
  `tests/parser/test_integration.py`) — individually classified above as either long-pre-existing
  (11) or mechanical, correctly-explained ripple effects of this arc's own legitimate fixes (6).
  Recommend a dedicated, explicitly-scoped follow-up cleanup task to reconcile these hardcoded
  counts (and separately investigate the genuine `password`-type data-item parser gap, which is not
  a stale count and needs its own fix), rather than folding that work into Task 4a.
- No dedicated unit test isolates the Block→Recover *consultation-timing* fix itself in isolation
  (only indirectly exercised via the real-sample reachability assertions) — a lower-priority
  test-coverage gap, not a correctness concern given this review's direct ablation verification.

## Verdict

**Pass.** This is the first of six review cycles on Task 4a to find the Block→Recover reachability
defect genuinely fixed, verified by ablation in both directions (not just claimed): disabling the
fix reproduces the prior, broken 16/8 result exactly; enabling it produces the claimed 21/3 result
with exactly the 5 pages the prior review had already hand-traced as real, wrongly-gated BP
callers. The task's own literal Done-when acceptance criterion (`Get Mails`/`Populate Queue`
reachable, the 2 named orphan pages unreachable) is met, `tests/ast/ -v` passes in full (133/133),
and a full 24-page sweep against architecture doc §B14/§B16 found no other page incorrectly
classified. The remaining 17 full-suite failures are, on individual investigation (not a blanket
sweep), either long-pre-existing and unrelated to this task's changes, or mechanical, arithmetically
-verified ripple effects of this arc's own legitimate fixes, landing exclusively in test files that
were never part of Task 4a's own declared file scope (`ast/builder.py`, `ast/models.py`,
`tests/ast/test_builder.py`, `tests/ast/test_models.py`). Per the explicit judgment call this
review was asked to make: requiring yet another Task-4a-scoped pass to reconcile hardcoded counts
in files outside that scope would not be a proportionate response. Recommend, as separate,
explicitly-scoped follow-up work (not gating Task 4a):

1. A dedicated cleanup task to reconcile `tests/parser/test_integration.py`'s four stale counts
   (`test_raw_page_count`, `test_normalised_stage_count`, `test_stage_type_counts[ACTION-174]`,
   `test_stage_type_counts[CALCULATION-42]`) against the now-correct 796/113/175/19 values, and to
   separately audit/refresh `tests/engine/test_flag_index.py`'s and
   `tests/mapper/test_vbo_router.py::TestIntegration`'s long-stale baselines.
2. A real investigation (not a count update) into the parser's failure to capture `password`-typed
   input parameters as `BPDataItem`s — `test_pid_0171_page_with_password_item_emits_sensitive`
   needs a parser fix, not a stale-count fix.
3. Flag `Close Down`'s likely third-orphan status to whoever owns architecture doc §B14 for
   confirmation, alongside the already-open uncertainty on rows 16/21.
4. Consider persisting the Block→Recover pairing onto `BPStage`/`BPPage` (or a returned side-map)
   so Task 4b/rendering doesn't have to reconstruct it from scratch — raised in three prior reviews,
   still open.
