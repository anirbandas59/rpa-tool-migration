# Review: Task 2a — 2026-08-28

**Overall: 78%**

First review of Task 2a (no prior report to reconcile against).

## Scope recap

Task 2a (`docs/pad-generation-task-prompts.md` lines 64-100) is a pure data-correction task against
`mapping/vbo_catalogue.yaml`: five specific `runtime`/`pa_module`/`notes` corrections plus a header
review-stamp, each required to trace to `docs/bp-to-pad-implementation-strategy-PID171.md` §2.2 and
`docs/pad-reference/vbo-action-mapping.md`. Explicitly out of scope: new VBO entries, method-pattern
fixes (Task 2b), `stage_rules.yaml` (Task 2c). "Done when": the file still parses as YAML and
`uv run pytest tests/mapper/test_config.py -v` passes unmodified.

Because this task touches no flow/workflow generation output, `FUNCTION` bodies, PAD identifier
naming, or error-handling constructs, most strategy-doc §4 dimensions are N/A — scored below,
weighted mainly on VBO/action fidelity (the one directly applicable dimension) plus a verified
test-regression finding.

## Files actually changed (verified via `git diff --stat HEAD` / `git diff HEAD`)

- `mapping/vbo_catalogue.yaml` only (+8/-8 lines) — correctly scoped, no scope creep. `tests/mapper/`
  files untouched, matching the "unmodified" requirement.

## Independent verification performed

- `uv run python -c "import yaml; yaml.safe_load(open('mapping/vbo_catalogue.yaml'))"` → succeeds
  (`YAML OK`).
- `uv run pytest tests/mapper/test_config.py -v` → **40 passed** (matches the implementer's claim).
  Process exit code is 1 due to the pre-existing global `fail_under=85` coverage gate applying to a
  narrow test-file run — this is the same pre-existing, not-chargeable artifact already documented
  in `docs/reviews/1-2026-08-28.md` finding 4 and reconfirmed in the Task 1 follow-up review, not a
  defect introduced by this task.
- Read the full diff (`git diff HEAD -- mapping/vbo_catalogue.yaml`) and cross-checked all 6 required
  changes against `docs/pad-reference/vbo-action-mapping.md` and, for the two most consequential
  corrections, directly against the raw reference `.robin.txt` files (not just the summary table):
  - `WorkQueues.ProcessWorkQueueItem.ProcessWorkQueueItem` confirmed at
    `DF_PID_171_US_LIMS_Prelude_Main.robin.txt` L1371 inside a `FUNCTION ... GLOBAL` block (a Desktop
    Flow construct) — supports the `clsWorkQueuesActions` runtime CLOUD→DESKTOP correction.
  - `External.RunFlow FlowId: '15ac1877-...' ... @In_txt_Payload: ... @Out_txt_Payload=>` confirmed at
    the same file, L1546, inside the `Ctrl_SendDataToGateway`-gated `BLOCK 'Data gateway block'` —
    supports the `Utility_DataGateways-CustomFramework` → `pa_module: External` correction and its
    rewritten notes verbatim.
- Ran the wider, natural test scope (`uv run pytest tests/mapper/ -v` and `uv run pytest tests/ -q`)
  — **not** run or disclosed by the implementer, whose summary only reports the narrowly-scoped
  `test_config.py` command. This surfaced a real, directly-caused regression (see gap 1 below).

## Dimension scores

| Dimension | Score | Evidence |
|---|---|---|
| Structural | N/A | Task touches no flow/workflow generation output — pure `mapping/*.yaml` data edit. |
| `FUNCTION` functional fidelity | N/A | Task touches no `FUNCTION` body generation. |
| VBO/action fidelity | 80% | All 5 required corrections are present, accurate, and correctly cited: `clsWorkQueuesActions` runtime CLOUD→DESKTOP (verified against `DF_PID_171_US_LIMS_Prelude_Main.robin.txt` L1371), `Utility - JSON` runtime CLOUD→DESKTOP (Loader L24/L116), `Utility_DataGateways-CustomFramework` `pa_module: Database→External` with notes correctly rewritten to describe the `External.RunFlow` pattern (verified L1546), `Utility - Environment` `pa_module: shared_sharepointonline→System`, and `clsCredentialsActions` notes correctly rewritten to explain the `External.InvokeCloudConnector` pattern and the binary-runtime-field limitation without inventing a new schema field (per task's explicit instruction not to). Deducted for: (a) a **verified test regression** — `tests/mapper/test_vbo_router.py::TestKnownVBORouting::test_work_queues_vbo_is_cloud_runtime` explicitly asserts `Runtime.CLOUD` for `clsWorkQueuesActions` and now fails, because that test encodes exactly the old, wrong assumption this task was scoped to correct; not caught, not disclosed, and not flagged as an "open question" despite being a direct, mechanical consequence of the required change; (b) a stale, now self-contradictory note left on the same `clsWorkQueuesActions` entry the implementer edited (see gap 2); (c) `Utility - JSON`'s notes still describe a "Cloud Flow ParseJson/Compose" cloud path that no longer applies now that runtime is DESKTOP-only, left unrewritten (task didn't explicitly require rewriting this note, so this is a minor/soft ding, not primary). |
| Naming convention | N/A | No PAD identifiers/`In_`/`Out_`/`GLOBAL.` qualification touched by this task; the `pa_module` string values used (`System`, `External`, `WorkQueues`) are consistent with real PAD module names cited in `vbo-action-mapping.md`. |
| Error-handling/stop/consecutive-exception | N/A | Not relevant to a VBO-catalogue data-correction task. |
| Well-formedness | N/A (task emits no XML/JSON/`<Definition>` artifact) — supplementary checks pass | YAML parses cleanly (verified directly, not just trusted from the summary); `tests/mapper/test_config.py` (the task's literal "Done when" target) passes 40/40 unmodified, matching the requirement that this is "data only, not schema." However, the wider `tests/mapper/` suite (not the literal Done-when command, but the natural scope for a change of this kind per `.claude/rules/mapping-yaml-conventions.md`'s "confirm... the relevant tests/mapper/ suite passes") has 1 failing test directly caused by this change, and the full `tests/` suite is otherwise clean (719 passed, 1 failed, 94% coverage) — confirming the regression is real, isolated, and reproducible, not a flake. |

## Most significant gaps

1. **Verified test regression not caught or disclosed.**
   `tests/mapper/test_vbo_router.py::TestKnownVBORouting::test_work_queues_vbo_is_cloud_runtime`
   (lines 130-134) asserts `decision.runtime == Runtime.CLOUD` for
   `Blueprism.Automate.clsWorkQueuesActions` — the exact old, incorrect assumption Task 2a's item 1
   was scoped to fix. Reproduced directly: `uv run pytest tests/mapper/ -v` → `1 failed, 165 passed`.
   The task's literal "Done when" command (`tests/mapper/test_config.py` only) doesn't happen to
   exercise this test, so the implementer's narrow command technically "passed," but the summary's
   "Deviations from spec: None... Open questions: None" is not accurate — a downstream test the
   correction directly breaks should have been surfaced as an open question/handoff note for
   whoever updates `test_vbo_router.py` (arguably Task 2b or a fast-follow, since Task 2a's file
   scope is `mapping/vbo_catalogue.yaml` only and correctly does not include touching test files).
   This mirrors a pattern flagged twice already in the Task 1 reviews (`docs/reviews/1-2026-08-28.md`
   finding 3, `1-2026-08-28-followup.md` finding 2-3): implementer summaries in this project have a
   recurring habit of claiming full completeness without running/disclosing the natural broader test
   scope.
2. **Stale, self-contradictory note left in the edited `clsWorkQueuesActions` entry.** Lines 342-344
   still read: *"NOTE: Defer Queue Item maps specifically to DESKTOP runtime (PAD
   WorkQueues.DelayWorkQueueItem), not CLOUD, even though the overall VBO runtime is CLOUD."* This
   sentence made sense when the entry's `runtime` was `CLOUD` (contrasting one method's desktop
   behavior against the VBO's overall cloud runtime); now that the entry's `runtime` field is itself
   `DESKTOP` (this task's own correction), the note is internally contradictory — it asserts "the
   overall VBO runtime is CLOUD" three lines below a field that says `DESKTOP`. Not required
   verbatim by the task's step 1 (which only specifies the `runtime` field change), but leaving
   demonstrably false prose in an entry the task edited undermines the catalogue's trustworthiness as
   a "single source of truth" data file per `.claude/rules/mapping-yaml-conventions.md`.
3. **Minor: `Utility - JSON`'s notes retain a stale "Cloud Flow ParseJson/Compose" cloud-path
   reference** (lines 154-157) that no longer applies now that the runtime correction confirms this
   VBO's real usage is DESKTOP-only. Not explicitly required by the task's item 2, but the same
   internal-consistency concern as gap 2, on a smaller scale.
4. **`Utility - Environment`'s notes were also left stale** ("Maps to reading from a SharePoint
   config list...") despite `pa_module` being corrected away from `shared_sharepointonline` to
   `System` — same category as gaps 2-3, task's item 4 only required the `pa_module` field change so
   this is a minor/soft ding, not a hard requirement miss.

## What's solid

- All 5 required field-level corrections are present, correct, and match the real ground truth in
  `docs/pad-reference/vbo-action-mapping.md`/the raw `.robin.txt` reference files (independently
  spot-checked, not just trusted from the citation text).
- `clsCredentialsActions` handling is exactly right: kept `pa_module`/`runtime` unchanged as
  instructed, did not invent a new schema field to resolve the binary-runtime limitation (correctly
  deferred, as the task explicitly required), and the rewritten note accurately documents the
  `External.InvokeCloudConnector` pattern per `vbo-action-mapping.md` line 65.
- File-scope discipline is clean — only `mapping/vbo_catalogue.yaml` touched, `tests/mapper/`
  correctly left alone (matching the "unmodified" requirement in "Done when").
- Header review-stamp comment added exactly as item 6 required.
- The task's literal "Done when" command was actually run and does pass as claimed (40/40,
  unmodified schema).

## Verdict

needs another implementation pass (small) — the 5 core data corrections are accurate and
well-cited, but this task leaves one directly-caused, verified test regression
(`test_work_queues_vbo_is_cloud_runtime`) undisclosed, plus a self-contradicting leftover note in
the same entry it edited. Neither requires re-doing the core corrections; recommended fix is a
narrow follow-up (either as part of Task 2b, since it already depends on this data and touches the
catalogue further, or a tiny dedicated fix) to: (a) update or remove the now-incorrect
`test_work_queues_vbo_is_cloud_runtime` assertion, and (b) clean up the stale
"overall VBO runtime is CLOUD" sentence on the `clsWorkQueuesActions` entry so the catalogue doesn't
contain internally contradictory prose. Does not block Task 2b from proceeding on top of this data.
