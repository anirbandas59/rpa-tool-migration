# Review: Task 2b — 2026-08-29

**Overall: 63%**

First review of Task 2b (no prior report to reconcile against).

## Scope recap

Task 2b (`docs/pad-generation-task-prompts.md` lines 312-367) is a pure data-addition/correction
task against `mapping/vbo_catalogue.yaml` only: add `PID_0003_Object_US_SampleManager` /
`PID_0005_Object_US_SampleResultsEntry` (UI-automation stubs), `MS Outlook Email VBO Extended` /
`MS Outlook Extended VBO`, `Utility_Object_Generic_ScreenShot`, `Utility - Image Search`,
`Utility_Sharepoint_API_Config`, and fix `MS Excel VBO`'s `method_patterns`/`method_actions`/
`fusion_patterns`. Every template must be copied verbatim from
`docs/pad-reference/vbo-action-mapping.md`, never invented. "Done when": file still parses as YAML
and `uv run pytest tests/mapper/ -v` passes.

Because this task touches no flow/workflow generation output, `FUNCTION` bodies, or PAD identifier
naming, most strategy-doc §4 dimensions are N/A — scored mainly on VBO/action fidelity (the
primary content of this task) plus well-formedness/test-regression findings.

## Files actually changed (verified via `git diff acae3f5 -- mapping/vbo_catalogue.yaml`)

`mapping/vbo_catalogue.yaml` only — correctly scoped, no scope creep. Diff is purely additive
except the two intentional `MS Excel VBO`/`Utility_Sharepoint_API_Config` field updates the task
required. Entry count: 32 → 38 (6 new entries), matching the implementer's own count.

## Independent verification performed

1. **YAML parses**: `uv run python -c "import yaml; yaml.safe_load(open('mapping/vbo_catalogue.yaml'))"` → succeeds.
2. **`uv run pytest tests/mapper/ -v`**: reproduced exactly as reported — **3 failures**, all
   `assert 38 == 32` in `tests/mapper/test_config.py` (`test_load_default_mapping_dir`,
   `test_load_real_vbo_catalogue`, `test_real_vbo_catalogue_loadable`), nothing else broken. This
   confirms the implementer's disclosed scope-conflict claim is accurate, not a mischaracterization
   of a different defect — the 3 assertions are literally hardcoded `== 32` counts, and this task's
   declared file scope (`mapping/vbo_catalogue.yaml` only) genuinely cannot fix them without
   touching a test file outside scope.
3. **`uv run pytest tests/e2e/test_pid171_pipeline.py -v`**: all 16 tests pass (structural/JSON/XML
   well-formedness, `# TODO`/`Definition` checks) — confirms the catalogue addition doesn't break
   the full pipeline.
4. **Verbatim-citation check, every added `method_actions`/`fusion_patterns` value against the
   actual text of `docs/pad-reference/vbo-action-mapping.md`** (not the summary's paraphrase):
   - Excel: all 5 values (2 `fusion_patterns` + 3 `method_actions`) are **exact, character-for-character
     matches** of the Excel table rows (L39-43) — including both fusion pairs cited correctly,
     which the session flagged as a recurring problem area elsewhere. This is done right.
   - Outlook (both `MS Outlook Email VBO Extended` and `MS Outlook Extended VBO`): the 3 templates
     each have `External.InvokeCloudConnector` prepended, which is *not* literally present in the
     table's `Mark Email As Read`/`Move Email` rows (those rows use `...` to continue from the row
     above) — a reasonable, non-fabricated completion of the row's own ellipsis convention, not an
     invented action. Acceptable but not strictly "verbatim."
   - `Utility_Object_Generic_ScreenShot`'s `"Take Screenshot": "Workstation.TakeScreenshot.TakeScreenshotAndSaveToFile"`
     has **no corresponding row in `vbo-action-mapping.md` at all** — grepped the full file, no
     `Screenshot` entry exists anywhere in it. The catalogue entry's own `notes` field falsely
     claims "per vbo-action-mapping.md (Task 2b)". The real confirmed call *does* exist, but only in
     the raw ground-truth source the mapping doc is built from —
     `DF_PID_171_US_LIMS_Prelude_Main.robin.txt` L1361: `Workstation.TakeScreenshot.TakeScreenshotAndSaveToFile
     File: txt_LastScreenshotPath ImageFormat: System.ImageFormat.Png` — and the catalogue's
     template is missing both parameters (`File:`, `ImageFormat:`) that the real call carries. (Note:
     the task prompt itself only specifies the bare action name without params, so this gap
     originates partly in the task text — but the false "per vbo-action-mapping.md" citation is the
     implementer's own addition, not the task's wording.)
   - `Utility - Image Search` and `Utility_Sharepoint_API_Config`: correctly left without invented
     `method_actions`, with accurate `**STOP**`-style / superseded-by-Cloud-Flow notes. The
     `§A2`/`§B14 rows 22/24` citation for `Utility_Sharepoint_API_Config` checks out against the
     architecture doc (rows exist, describe the same config-read functionality being folded into
     `CF_PID_171_US_Read Config File`).
5. **Real-sample ground-truth check (not requested by name in the review brief, but essential to
   judging whether the additions actually work) — grepped `samples/blueprism/PID_0171.bprelease`
   for the exact `resource object="..."` attribute values the new catalogue entries are meant to
   match**:
   - `PID_0003_Object_US_SampleManager` — exact match, confirmed present verbatim in the sample.
   - `MS Outlook Email VBO Extended`, `MS Outlook Extended VBO`, `Utility_Object_Generic_ScreenShot`,
     `Utility - Image Search`, `Utility_Sharepoint_API_Config` — all exact matches, confirmed present
     verbatim.
   - **`PID_0005_Object_US_SampleResultsEntry` does NOT match.** The real `resource object="..."`
     attribute in the sample (confirmed at every occurrence, e.g. line 18) is
     `PID_0005_Object_US_ SampleResultsEntry` — note the embedded space after `US_`. The catalogue
     key omits it. `VBORouter.route()`'s fuzzy fallback
     (`src/flowsmith/mapper/config.py::get_vbo_entry_fuzzy`) is case-insensitive only, not
     whitespace-normalizing, so this mismatch is **not** absorbed by the fuzzy path either.
     Independently ran the annotator against the real `PID_0171.bprelease` sample and confirmed:
     every `PID_0005_Object_US_ SampleResultsEntry` ACTION stage still resolves as an **unknown VBO**
     (confidence `0.0`, `MANUAL` band, `Unknown VBO ... not in catalogue` error flag) — identical
     to pre-Task-2b behaviour. **The new entry is a functional no-op for the one real sample it
     exists to cover.** (`PID_0003_Object_US_SampleManager`, by contrast, correctly resolves at
     confidence `0.15`/`MANUAL` as intended; `MS Outlook Extended VBO` correctly resolves at `0.8`/
     `SPOT_CHECK`.) The task prompt itself specified the no-space name, so the premise error isn't
     solely the implementer's, but the catalogue file's own header states `vbo_name — exact string
     from resource/@object attribute in BP XML`, and this wasn't verified against the raw sample
     before being written — a one-line `grep` would have caught it, and the fix is trivial and
     within this task's own declared file scope (just correct the key string).

## Dimension scores

| Dimension | Score | Evidence |
|---|---|---|
| Structural | N/A | Task touches no flow/workflow generation output — pure `mapping/vbo_catalogue.yaml` data edit. |
| `FUNCTION` functional fidelity | N/A | Task touches no `FUNCTION` body generation; `method_actions`/`fusion_patterns` are not yet consumed anywhere in `src/flowsmith/generator/` (grepped — zero matches), so this data is inert until a future generator task wires it in. Expected for this task's scope, not a defect. |
| VBO/action fidelity | 60% | Excel: 5/5 templates verbatim-exact (fusion pairs correctly cited per §B_FUSION — a known problem area elsewhere in this project, done right here). Outlook: 6 values (3 × 2 duplicate entries) faithfully reconstructed from the doc's `...`-continued rows, not strictly verbatim but not fabricated either. `Utility - Image Search`/`Utility_Sharepoint_API_Config`: correctly handled per spec, no invented actions. Against this: (a) `Utility_Object_Generic_ScreenShot`'s note falsely cites `vbo-action-mapping.md` as the source of a template that doesn't exist anywhere in that file, and the template itself is missing parameters present in the real confirmed call it should have been drawn from instead (`DF_PID_171_US_LIMS_Prelude_Main.robin.txt` L1361); (b) most significantly, the `PID_0005_Object_US_SampleResultsEntry` entry — one of only two UI-automation-stub entries this task exists to add — never actually fires against the real `PID_0171.bprelease` sample because the real resource name has an embedded space the catalogue key omits, confirmed by running the annotator directly; every real PID_0005 stage still routes as an unmapped/unknown VBO exactly as before this task. |
| Naming convention | N/A | No PAD identifiers / `In_`/`Out_` / `GLOBAL.` qualification touched — templates use generic angle-bracket placeholders (`<ins>`, `<var>`, `<path>`), not concrete generated-code names. |
| Error-handling/stop/consecutive-exception | N/A | Task doesn't touch `BLOCK`/`ON BLOCK ERROR`/consecutive-exception constructs (§A5-A7). Separately, the related `# TODO`/`STOP` non-silent-drop discipline (§B9) *was* correctly applied in the `PID_0003`/`PID_0005`/`Utility - Image Search` notes (even though the `PID_0005` entry itself doesn't fire, its notes text is correctly written). |
| Well-formedness | 80% | YAML parses cleanly; pydantic schema loads without error; the full `tests/e2e/test_pid171_pipeline.py` suite (16/16, XML/JSON well-formedness, `Definition` checks) still passes unaffected. However, the task's own literal "Done when" bar (`uv run pytest tests/mapper/ -v` passes) is **not met** — reproduced independently: exactly 3 failures, all `assert 38 == 32` hardcoded catalogue-length assertions in `tests/mapper/test_config.py`, nothing else broken. This is a genuine, correctly-disclosed scope conflict (fixing it requires touching a test file outside this task's declared `mapping/vbo_catalogue.yaml`-only scope) rather than a silent defect, which is why it isn't scored at 0%, but the literal completion bar is still unmet. |

## Most significant gaps

1. **`PID_0005_Object_US_SampleResultsEntry` catalogue entry is a functional no-op against the real
   sample.** The real BP resource name (confirmed via direct `grep` of
   `samples/blueprism/PID_0171.bprelease`, every occurrence) is
   `PID_0005_Object_US_ SampleResultsEntry` — with an embedded space the catalogue key omits. The
   router's fuzzy fallback is case-insensitive only, not whitespace-tolerant, so this entry never
   matches. Verified live: annotating the real sample still resolves every PID_0005 ACTION stage as
   an unknown VBO (confidence 0.0, error flag), identical to pre-task behaviour — the fix this task
   was meant to deliver for this VBO doesn't take effect. The task prompt itself used the no-space
   name (shared premise error, echoing this project's recurring "task premise didn't match ground
   truth" pattern — see git history's "fix Task 4a premise"), but the catalogue file's own header
   contract (`vbo_name — exact string from resource/@object attribute in BP XML`) wasn't verified
   against the raw sample, and the fix (correcting the key string) is trivial and stays inside this
   task's own declared file scope.
2. **False citation on the `Utility_Object_Generic_ScreenShot` entry.** Its `notes` field claims the
   `Take Screenshot` template is "per vbo-action-mapping.md (Task 2b)" — but no Screenshot row
   exists anywhere in that file. The real confirmed call (`DF_PID_171_US_LIMS_Prelude_Main.robin.txt`
   L1361) has two parameters (`File:`, `ImageFormat:`) the catalogue's bare
   `"Workstation.TakeScreenshot.TakeScreenshotAndSaveToFile"` template omits. The task prompt itself
   under-specified this template, so this is a shared gap, but the specific "per vbo-action-mapping.md"
   citation is the implementer's own fabrication and should be corrected or removed.
3. **Disclosed but unresolved "Done when" failure.** The 3 `tests/mapper/test_config.py` failures
   are real (reproduced independently) and correctly characterized by the implementer as a hardcoded
   `== 32` count vs. the new 38-entry catalogue, and correctly flagged as a scope-boundary conflict
   rather than silently worked around. This is a genuine improvement in disclosure discipline
   compared to the undisclosed regression found in the Task 2a review, but the task's literal
   completion bar is still not met and needs a coordinator decision (widen scope, or a tiny
   dedicated follow-up) before this can be called fully done.

## What's solid

- File-scope discipline is clean — only `mapping/vbo_catalogue.yaml` touched, matching the task's
  declared scope.
- Excel fusion-pattern citation is done correctly and precisely — both `sequence`/`fused_action`/
  `vestigial_stages` triples match the task spec and the source table exactly, in an area the
  project has previously flagged as error-prone.
- `Utility - Image Search` and `Utility_Sharepoint_API_Config` correctly follow the "don't invent,
  mark low-confidence and STOP/superseded" discipline the task required, with accurate architecture-
  doc citations.
- `Utility_Sharepoint_API_Config` was already present under its exact real name before this task
  (confirmed via `git diff acae3f5`); the task prompt's step 5 wording ("add... distinct from...")
  implied it needed to be newly created, but that premise was wrong — updating the existing entry in
  place (rather than creating a spurious duplicate) was the correct call, and the implementer's
  summary disclosed this honestly rather than silently claiming a "new entry" that would have been
  a duplicate key.
- The 3 test failures were run, reproduced, and disclosed accurately rather than glossed over.

## Verdict

needs another implementation pass — the Excel work and the "don't invent" discipline on the
low-confidence entries are solid, but the `PID_0005` entry (one of only two entries the task's
step 1 exists to add) doesn't actually fire against the real target sample due to an unverified
whitespace mismatch, and the Screenshot entry carries a false documentation citation. Both are
small, mechanical fixes fully within this task's own declared file scope (correct the `vbo_name`
key string; fix/remove the false citation and complete the Screenshot template's parameters from
the real confirmed call). The hardcoded-count test failures are a genuine, correctly-disclosed
scope conflict — recommend the coordinator authorize a narrow follow-up (updating the 3 `== 32`
assertions to `== 38`, or deriving the count dynamically) rather than re-litigating it as a defect
of this task.
