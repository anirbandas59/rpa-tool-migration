# Review: Task 7d — 2026-09-25

First review of Task 7d. Reviewed the uncommitted working-tree changes on `feature/bp-to-pad-pipeline`
against base HEAD `d936d45`. I regenerated a HEAD baseline from a `git archive` extract in the
session scratchpad so I could compare before and after output. I did not use a worktree or stash,
so the working tree was not touched.

**Overall: 58%**

(This is the mean of the three dimensions that apply. Only 1 of the 4 items fully meets its
done-condition: item 1. Item 2 is half done. Items 3 and 4 do not meet theirs.)

| Dimension | Score | Evidence |
|---|---|---|
| Structural | 65% | **Item 1 passes.** The regenerated `.zip` has exactly 3 `Workflows/*.json` files (2 DF + 1 CF) and no `Populate_Queue_cloudflow.json`. The HEAD baseline still ships the orphan (4 files, including `Populate_Queue_cloudflow.json`, 1940 B). **Item 2 is half done.** The CF's `<JsonFileName>` file (`CF_..._Cloud_Main-<GUID>.json`, 37,540 B) now holds the real Logic-App JSON (`properties`/`schemaVersion`), and its `uiFlowId` GUIDs match the two DF `WorkflowId`s. At HEAD it was a 176 B `{"package": ""}` stub. However, the Category-5 `<Workflow>` **still carries an inline `<Definition>`** (42,460 chars, byte-identical content to the file). §A1 (architecture doc L50-52) says Cloud Flows "store no inline `<Definition>`". The reference `_11_managed/customizations.xml` confirms this: all 3 Category-5 entries have `Definition: absent`. The output is now duplicated rather than inverted. The done-condition ("storage matches the reference's convention") is not met. The cause is that `templates/report/customizations_workflows.xml.j2:35` emits `<Definition>` unconditionally. That template is outside the declared file scope, and the implementer did not report the scope limit ("Deviations: none"). Two further issues: the orchestrator is identified by the name heuristic `"Cloud_Main" in name` (`packager.py:227`), so any other Category-5 flow (e.g. the future config-read child CF) would still get a hollow manifest stub; and `generate_orchestrator` is now called twice per package (once in `build_all_workflows_consolidated`, once in the zip loop). The output is currently deterministic (file == Definition), but this is fragile. |
| FUNCTION functional fidelity | 25% | **Item 3 fails.** `Load Config Data` does not appear in either regenerated `.robin`. `grep "FUNCTION 'Load Config Data'"` finds 0 matches. The new `page_target_map.yaml` entry is keyed on a BP page name that **does not exist**. The AST's 24 page names (Main Page + 23 subsheets) contain no `Load Config Data`, so the entry is never looked up and has no effect. `Load Config Data` is a PAD-side FUNCTION with no 1:1 BP page. §B14 rows 22/24 fold the BP config pages into `CF_..._Read Config File`, and §A2 L118 / §B12 L730 say the FUNCTION is built from `obj_Config` parsing plus Environment-exposed DATA items. Generating it therefore needs a design decision (a synthesized FUNCTION sourced from Main Page's config stages and env-exposed DATA items), not a map entry. The entry has three further problems: its `notes` are wrong ("Cloud Flow invocation from Cloud Main orchestrator"), because it is a Desktop `FUNCTION ... GLOBAL` in **both** Loader L213 and Main/Performer L118, and the entry names no role; its citation "§B14 row reference — Loader.robin L213" is not a §B14 row number, as the file header requires; and §B14 itself is a real section (architecture doc L759), but no row covers this FUNCTION. **Item 4 fails.** Before and after, the regenerated output has identical placeholder counts: `%SomeVar%` Loader 14→14, Performer 73→73. The only diff is an added preceding comment line. The task says emit the TODO "*instead of*" the placeholder, but `pad.py:3074` still sets `final_value = "%SomeVar%"`, and DECISION still renders `"%SomeVar% = True"` (`pad.py:3166`, `:3988`). All 87 emitted TODOs are the new `"# TODO: <name> requires initialization — provide value or expression"` variant. That variant covers **DATA stages with an empty BP `initial_value`**, which is not the "Task 5b translator returned empty for a BP expression" case. None of them names a raw BP expression. That path is also the wrong target: §B12 L730 says a DATA item with no `initial_value` should emit **nothing** (or come from `obj_Config` if Environment-exposed, e.g. `Generic_SupportTeam_EmailID`, `PID_171_US_EV_LIMS_Prelude_ConfigFile`), so a TODO asking for "initialization" misdirects the developer. The genuine "could not translate BP expression '...' on stage '...'" branches (`pad.py:3065`, `:3160`, `:3993`) fire 0 times in PID_0171 and are uncovered by tests (coverage report lists 3065, 3160, 3993 as missed). One unfilled template skeleton also remains in a non-expression path: Performer L377 `# External.CALL '<page/subprocess>' ... \| Workflow <action> ...`. |
| VBO/action fidelity | N/A | N/A for this task's scope: no `CALL`/module-action templates were added or changed. |
| Naming convention | N/A | N/A for this task's scope: no variable/parameter naming changes. The `Load Config Data` key matches the reference FUNCTION name verbatim, but it has no effect (see above). |
| Error-handling/stop/consecutive-exception | N/A | N/A for this task's scope. |
| Well-formedness | 85% | Checks I ran myself: `uv run ruff check src/ tests/` reported "All checks passed!", and `ruff format --check` reported "58 files already formatted". Full suite: **877 passed, 10 failed**, the same 10 pre-existing `test_flag_index.py::test_real_*` (8) and `test_vbo_router.py::TestIntegration` (2) failures as prior reviews. Coverage is 93.46%, and all `tests/e2e/test_pid171_pipeline.py` tests pass. On the regenerated zip, `customizations.xml` parses, the CF JSON file parses, and the DF/CF GUID cross-references resolve. Deductions: **no tests were added** (`git diff HEAD -- tests/` is empty) despite "implement and test each separately". No existing tests were weakened or deleted. The three genuine item-4 branches are untested, and nothing guards against the orphan or against a stub/`<Definition>` regression. `tests/e2e/test_pid171_pipeline.py:46` still calls `CloudFlowGenerator().generate_process()`, so the e2e test now exercises a CLI shape that no longer exists. It passes only because `packager.py:140` silently discards `cf_files` (`cf_files = []`) with no log or warning. The CLI still creates and passes a now-always-empty `cloudflow/` dir (`cli/app.py:58-59`). |

## Most significant gaps
1. **Item 3 (done-condition failure):** `Load Config Data` is not generated. The new map entry is keyed on a BP page that doesn't exist in `PID_0171.bprelease`, so it is dead config. Generating this FUNCTION needs a design decision on how to synthesize it (a source-stage set and a Loader+Performer role, per §A2 L118 / §B12 L730 / §B14 rows 22 and 24). The entry's `notes` ("Cloud Flow invocation…") and citation ("§B14 row reference", not a row) are also wrong.
2. **Item 4 (done-condition failure):** the placeholder values are still emitted (`%SomeVar%` 14/73 unchanged from HEAD; `%SomeVar% = True` for DECISION). The TODO is added *alongside* them, not *instead of* them. All 87 emitted TODOs sit on the DATA-no-initial-value path, which is not the untranslated-expression path, and none names a raw BP expression. Per §B12 L730 that path should emit nothing or come from config. The real "could not translate BP expression" branches never fire on PID_0171 and are untested.
3. **Item 2 (partial):** the CF `<JsonFileName>` payload is now correct, but the inline `<Definition>` is still present for Category 5, contrary to §A1 and the reference customizations.xml. The fix needs `templates/report/customizations_workflows.xml.j2` (or `workflow_builder.py`), which is outside the declared scope. The implementer should have reported this instead of claiming "no deviations". Orchestrator detection by `"Cloud_Main" in name` is a name heuristic.
4. **No tests added for any of the 4 items.** 877→877 passed because nothing was added. None were removed or weakened.
5. **Item 1 (minor):** the fix is correct, but the packager's `cf_files = []` silently discards any caller-provided CF files. The e2e test still calls the removed CLI step and is masked by that discard. The CLI still creates a vestigial empty `cloudflow/` dir.
6. **Implementer summary accuracy:** it claims item 3 was "added with proper citation" and item 4 was "73+ TODO instances". The first is inaccurate. The second is literally true, but those TODOs come from a different path than the one the task named, and the example output the summary quotes itself shows the placeholder retained.

## Verdict
**Needs another implementation pass.** Items 3 and 2 are **blocked pending a design/scope decision**:
- Item 3: how `Load Config Data` is synthesized when there is no BP source page.
- Item 2: widening the scope to the customizations template, or to `workflow_builder.py`, so `<Definition>` can be omitted for Category 5.

Item 4 can be fixed within scope:
- Drop the placeholder and emit only the TODO on genuine translation failure.
- Stop emitting `SET … TO %SomeVar%` for DATA items with no initial value, per §B12 L730.
- Add tests.

Item 1 is acceptable. It needs a regression test, and ideally the stale e2e call should be removed.
