# Review: Task 7c — 2026-09-24

First review of Task 7c. The change is uncommitted in the working tree (`pad.py`, `templates/pad/flow_header.robin.j2`, `tests/generator/test_pad.py`). I graded it against the task text and the four coordinator constraints.

**Overall: 78%**

| Dimension | Score | Evidence |
|---|---|---|
| Structural | N/A | N/A for this task's scope. The change only touches the header. I regenerated `PID_0171` from HEAD and from the working tree and diffed them: the bodies of both `.robin` files are identical, and only lines 8-14 of the header changed. |
| FUNCTION functional fidelity | N/A | N/A for this task's scope. No `FUNCTION` body changed (confirmed by the HEAD-vs-working-tree diff). |
| VBO/action fidelity | N/A | N/A for this task's scope. |
| Naming convention | 75% | **Pass:** `@INPUT` count went from 92/111 (HEAD regen) to exactly 1 per file. `cmp` against reference L8 in both `DF_PID_171_US_Loader.robin.txt` and `DF_PID_171_US_LIMS_Prelude_Main.robin.txt` shows the line is byte-identical for Loader and Performer. The contract lives in the Jinja template, not in Python. The per-item union is removed from both call sites (`generate_page` L259-277 and `_generate_consolidated_flow` L360-377; the implementer's "`_generate_role_flow`" does not exist). **Partial:** constraint 2 requires the TODO to name the BP input *and its PAD variable name*. The template prints only `data_item.name`, which is the BP Start-parameter name, not the `inputs_stage_map`-bound data item and not the `_apply_type_prefix` PAD name. On PID_0171 all three happen to be the same (`flg_SendDatatoDataGateways`). On the existing 7b0 fixture (`("SendFlag","flag","send_flag")`) my probe of `_generate_consolidated_flow` printed only `# TODO: BP process input 'SendFlag' (Bool)`, so the body variable `send_flag` is not named. The TODO text also contains wrong statements (see gaps 3-4). |
| Error-handling/stop/consecutive-exception | N/A | N/A for this task's scope. The reference's `IsEmpty(obj_Config)` guard in Initialise Values is not in 7c's scope. |
| Well-formedness | 90% | `tests/e2e` 17/17 pass on the regenerated output. The full suite (`--no-cov`) is 786 passed with exactly the 3 baseline `test_vbo_router.py::TestIntegration` failures. `test_pad.py` is 119 passed / 4 skipped, matching the claim. `ruff check` is clean. Deduction: the change inserts a new `# Reference: docs/pad-reference/...` line between `@SENSITIVE` and `@INPUT` in every shipped `.robin`. The reference never has a comment inside the directive block (L1-L10 are all directives, and the first comment is L11). No proven check validates that placement, and it leaks a repo-internal path into customer artifacts. It should be a Jinja `{# #}` comment. |

## Most significant gaps
1. **The shipped path (`_generate_consolidated_flow`) has no test coverage for 7c.** All 3 new tests call `generate_page`, which is legacy and not used for the real output. I ran the mutations on a scratch copy, with the full suite each time:
   - M1 (restore the old union in the role flow: all Main stages plus sub-pages) → **786 pass / 3 baseline, survives.**
   - M3 (role flow passes `inputs=[]`, so it silently drops the Main START TODO, which is constraint 2's core requirement) → **survives.**
   - M2 (`generate_page` all-stages union) → killed.
   - M4 (`generate_page` drops inputs) → killed.
   - M5 (edit `DefaultValue` in the template) → killed.

   So "mutation-checked" (constraint 4) holds only for the legacy path. Neither the Main-START vs non-START distinction nor the Main vs sub-page distinction is tested on the role-flow path (coordinator item f). No old tests were removed (the diff is additions only), so nothing was lost there.
2. **The required open question was not reported.** The implementer wrote "Open questions: None", but constraint 2 explicitly asked them to report that Start-bound variables are now neither `@INPUT` nor hoisted-initialised. On PID_0171, `flg_SendDatatoDataGateways` (BP Data stage: `initialvalue False`, `alwaysinit`) now has no declaration and no `SET` anywhere in either `.robin` (grep: it appears only in the TODO line). It is **not read** in the generated body today, because its only reader is the `Send Data to Gateways` decision (`[flg_SendDatatoDataGateways]`) on the `DataGateway` page, which is not rendered. So nothing reads it uninitialised yet, but the gap is latent: it will show up as soon as that page is rendered or 7d wires the config parse. `test_7b0_main_hoisting_excludes_flow_input_bound_names` (L4177-4200) and `_get_input_bound_names`' docstring still describe the variable as bound "to the flow's own `@INPUT`", which is no longer true.
3. **The TODO text contains false or mis-cited claims** (coordinator item b):
   - §B9 is "Scope & ground rules", mainly the UI-selector TODO rule, and says nothing about `In_txt_Config`. Only §A4 is relevant.
   - It says the blob is "parsed … in the Load Config Data stage". In the reference, the parse (`Variables.ConvertJsonToCustomObject Json: In_txt_Config`) is in the *Initialise Values* region: Loader L24 and Main L35. `Load Config Data` is a separate function (Loader L213).
   - It says "Item 3 of Task 7d will generate the config-parse action". Task 7d item 3 only adds the `Load Config Data` `page_target_map.yaml` entry, and no task generates the Initialise-Values parse.

   Every generated file now carries this misinformation.
4. **An out-of-scope `@OUTPUT` loop was added to the template** (coordinator item a). HEAD's template had no `@OUTPUT` rendering at all. Both call sites still pass `outputs=[]` (verified at L277 and L399), so the rendered output does not change today. But the task marks `@OUTPUT` out of scope, constraint 3 says to leave it unchanged, and the new format is not cited to the reference. It sets `FriendlyName` to the variable name verbatim, which matches Main L9-13 but **not** Loader L9-10 (`Out_num_MailItemCount` → `FriendlyName: 'out_num_MailItemCount'`). This is unreported speculative contract code and should be removed.
5. **The TODO omits the PAD variable name** (coordinator item e): see the Naming row. It should render the `inputs_stage_map`-resolved and type-prefixed name alongside the BP parameter name, as `_get_input_bound_names` / `_build_body_variable_overrides` already compute.
6. **The implementer summary is inaccurate:** "Deviations: None" is wrong given gaps 3-4, and the method name `_generate_role_flow` does not exist.

## Verdict
Needs another implementation pass. The core done-condition is met: exactly 1 `@INPUT` per file, byte-identical to reference L8, with the contract in the template. The remaining items are small but required:
- add role-flow tests (Main START, a sub-page START input that must be excluded, and a Main non-START input that must be excluded), mutation-checked against M1 and M3
- name the PAD variable in the TODO
- correct the TODO text (drop §B9, say "Initialise Values L24/L35", and no false 7d attribution)
- remove the `@OUTPUT` loop
- turn the `# Reference:` line into a Jinja comment
- report the uninitialised-Start-variable open question, and update the stale 7b0 test and docstring wording
