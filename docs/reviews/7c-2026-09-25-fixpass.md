# Review: Task 7c — 2026-09-25 (fix pass)

This is the second review of Task 7c. The prior review, `docs/reviews/7c-2026-09-24.md`, scored it 78% and returned "needs another implementation pass". The change is still uncommitted in the working tree (`pad.py`, `flow_header.robin.j2`, `test_pad.py`).

**What changed since the prior review:**
- **New helper.** `PADGenerator._collect_header_start_inputs(page, variable_name_mapping)` now builds the header TODO list for both call sites. It returns only START-stage inputs, and the PAD name is resolved through `inputs_stage_map`, then `variable_name_mapping`, then `_apply_type_prefix(bound, DataTypeMapper().map_type(bp_type).pad_type)`.
- **Mapping built earlier.** `variable_name_mapping` is now built before the header in `_generate_consolidated_flow`.
- **`@OUTPUT` loop removed.** The template no longer renders `@OUTPUT`, which matches HEAD.
- **Reference line is a Jinja comment.** The `# Reference:` line is now `{# #}`, so it no longer appears in shipped artifacts.
- **TODO text rewritten.** §B9 and the false Task 7d attribution are gone. The text now cites "Initialise Values region — Loader L24, Main L35" and §A4. It now names the PAD variable, and it states the open question: the parse is not generated and these variables are uninitialised.
- **Stale wording fixed.** The stale 7b0 docstring and test wording are updated.
- **Four new tests.** Three new tests cover the shipped `_generate_consolidated_flow` path: Main START input, sub-page START input excluded, and Main non-START input excluded. A fourth, added by the coordinator, checks that the TODO names the bound PAD variable rather than the parameter name.

**Overall: 97%**

| Dimension | Score | Evidence |
|---|---|---|
| Structural | N/A | N/A for this task's scope (header only). |
| FUNCTION functional fidelity | N/A | N/A for this task's scope. No `FUNCTION` body logic changed. |
| VBO/action fidelity | N/A | N/A for this task's scope. |
| Naming convention | 95% | **Pass:** After regenerating, each file has exactly 1 `@INPUT`. `cmp` shows the Loader line is byte-identical to reference `DF_PID_171_US_Loader.robin.txt` L8, and the Performer line is byte-identical to `DF_PID_171_US_LIMS_Prelude_Main.robin.txt` L8. The TODO names both the BP input and the PAD variable (`'flg_SendDatatoDataGateways' (Bool) → PAD variable flg_SendDatatoDataGateways`). The citations are correct: the `ConvertJsonToCustomObject Json: In_txt_Config` line is at Loader L24 and Main L35, and §A4 (arch doc L276+) covers `In_txt_Config`. "No task owns it" is accurate: Task 7d item 3 only adds the `Load Config Data` mapping entry. I ran 9 mutations on a scratch copy against the full suite, comparing against a 7-failure baseline caused by the scratch environment (CLI tests). All of the following were killed: M1 (union of all role pages' START inputs in the shipped path), M2 (shipped `inputs=[]`), M3 (PAD name taken from the parameter), M4 (template `'IsOptional': false`), M5 (helper drops the START-stage filter), M8 (the "not generated yet" caveat removed), M9 (the TODO omits the PAD variable). **Deduction:** M6 (the helper ignores `variable_name_mapping` and uses only the type-prefix fallback) and M7 (the shipped path passes `{}` instead of the mapping) both **survive**. They are not equivalent mutants. The mapping takes the type and casing from the DATA stage, while the fallback takes them from the Start parameter's type and the `stage=` string's casing. The gap-5 test fixture (`flag` parameter bound to a `Boolean` data item) produces the same result both ways. |
| Error-handling/stop/consecutive-exception | N/A | N/A for this task's scope. |
| Well-formedness | 100% | I ran every check myself: `test_pad.py` 123 passed / 4 skipped; full suite (`--no-cov`) 790 passed, with exactly the 3 baseline `test_vbo_router.py::TestIntegration` failures; `ruff check src/ tests/` is clean; `tests/e2e` 17/17 pass on freshly regenerated output. Every emitted directive (L1-L8) now comes before any comment, matching the reference's layout where the directives end before the first comment. |

## Most significant gaps
1. **Two mapping-precedence mutants survive (M6/M7).** No test fails if the header ignores `variable_name_mapping`. A fixture where the Start parameter's BP type differs from the bound DATA item's type would kill both, for example a `text` parameter bound to a `Boolean` data item: the mapping gives `flg_…` and the fallback gives `txt_…`. This is a small test gap; the code itself is correct.
2. **Open design question, outside 7c's scope, that the coordinator should act on.** No task in `docs/pad-generation-task-prompts.md` generates the Initialise Values `Variables.ConvertJsonToCustomObject Json: In_txt_Config CustomObject=> obj_Config` parse (reference Loader L24 / Main L35). As a result, Main START-bound variables such as `flg_SendDatatoDataGateways` are declared nowhere and never initialised; grep finds them only in the TODO line. Nothing in the generated body reads them yet, but the first render of `DataGateway` or any other reader will expose this. The TODO now documents it honestly. A task needs to be scheduled for it.
3. **Minor.**
   - The shipped TODO text says "(no task owns it)", which puts internal planning status into a customer artifact. It is accurate today but will go stale.
   - Both call sites still pass `outputs=[]`, which the template no longer uses. That is harmless dead kwargs.
   - Any future `@OUTPUT` work must render its lines before this TODO block, or comments will end up inside the directive block (reference L9-10 are `@OUTPUT`).
   - The docstring of `test_7c_main_start_input_produces_todo_comment` says "after @INPUT/@OUTPUT lines".

## Verdict
Pass. The done-condition is met: exactly 1 `@INPUT` (`In_txt_Config`) per generated file, byte-identical to reference L8. All 6 prior gaps are closed and verified, and the prior surviving mutations (union in the shipped path, and shipped `inputs=[]`) are now killed. Gap 1 is an optional test hardening. Gap 2 needs a new task from the coordinator; it does not block 7c.
