# Review: Task 2b (fix pass) — 2026-08-29

**Overall: 78%**

This is a re-review of the Task 2b fix pass, following the prior report
`docs/reviews/2b-2026-08-29.md` (63%, "needs another implementation pass"). Both content
defects flagged in that review are now genuinely fixed and independently verified end-to-end.
However, this pass surfaces a new, undisclosed finding: the fix-pass instructions explicitly
required running `uv run pytest tests/ -q` and reporting the exact count — the implementer's
summary did not do this. Running it myself found 2 real failures in `tests/engine/test_scorer.py`
(hardcoded real-sample confidence-distribution counts) that were *already present before this fix
pass* (traced to the original Task 2b catalogue corrections, not to Fix 1/2/3 specifically), but
were never caught or disclosed by either the original implementer or my own prior review, since
neither ran the full suite. That gap is now closed by this review, but the implementer of this
fix pass still failed to do what was explicitly asked.

## Scope recap

Task 2b (`docs/pad-generation-task-prompts.md` lines 312-367) is a pure data task against
`mapping/vbo_catalogue.yaml`. This fix pass had three assigned corrections: (1) fix the
`PID_0005_Object_US_SampleResultsEntry` key to match the real BP resource name (embedded space),
(2) correct the `Utility_Object_Generic_ScreenShot` template/citation, (3) resolve the disclosed
`tests/mapper/test_config.py` hardcoded-count scope conflict (`== 32` → `== 38`).

## Files actually changed (verified via `git diff` against the working tree, matching `git diff acae3f5` state reviewed previously)

`mapping/vbo_catalogue.yaml` and `tests/mapper/test_config.py` only — matches the fix-pass's
declared scope. Diff against the prior 63%-reviewed state is exactly the 3 disclosed fixes; every
other line (Excel fusion patterns, Outlook entries, PID_0003, Image Search, Sharepoint
confidence/notes update) is byte-identical to the state I reviewed previously. No scope creep.

## Independent verification performed

1. **Real resource-name grep** — `samples/blueprism/PID_0171.bprelease`:
   - `PID_0003_Object_US_SampleManager` (no space) — confirmed, unchanged from before.
   - `PID_0005_Object_US_ SampleResultsEntry` — confirmed via regex extraction, **exact
     byte-for-byte match** including the single embedded space after `US_`, no leading/trailing
     whitespace. Cross-checked the YAML's parsed string value with `yaml.safe_load` and it is
     `'PID_0005_Object_US_ SampleResultsEntry'` — identical Python `repr()` to the value extracted
     directly from the raw sample file. No YAML-quoting artifact.

2. **Live end-to-end router proof (the actual test of whether Fix 1 works)** — parsed the real
   `PID_0171.bprelease` via `parse_process` → `build_ast` → `create_annotator().annotate_process()`
   and searched for stages referencing `SampleResultsEntry`. Found **11 real stages** (`Launch
   SampleManager`, `Attach`, `Get Component Names`, `Enter Values`, `Close Reslut Entry Screen`,
   etc.), all now resolving to `target_module='UIAutomation'`, `confidence=0.15`,
   `band=ConfidenceBand.MANUAL`, `flags=[]` (no error flag). Before this fix, every one of these
   stages resolved as an unknown VBO at confidence `0.0` with an `Unknown VBO ... not in catalogue`
   error flag (confirmed in the prior review). This is genuine, confirmed proof the fix works, not
   just a string-literal change.

3. **Reference-file line check** — `docs/pad-reference/DF_PID_171_US_LIMS_Prelude_Main.robin.txt`
   L1361 reads exactly: `Workstation.TakeScreenshot.TakeScreenshotAndSaveToFile File:
   txt_LastScreenshotPath ImageFormat: System.ImageFormat.Png`. The new YAML template —
   `"Workstation.TakeScreenshot.TakeScreenshotAndSaveToFile File: <path> ImageFormat:
   System.ImageFormat.Png"` — matches this structurally (action name + both real parameters), using
   the catalogue's established `<path>`/`<var>`/`<ins>` placeholder convention consistent with
   other entries (e.g. the Excel templates). The citation now correctly names the raw reference
   file and line number instead of the previously-false `vbo-action-mapping.md` claim — confirmed
   `vbo-action-mapping.md` has **zero** occurrences of "screenshot" (grepped), so citing the raw
   `.robin.txt` source is the correct and only accurate citation available. Both flagged defects
   from the 63% review are resolved.

4. **`uv run pytest tests/mapper/ -v`**: **178 passed, 0 failed** — matches the implementer's claim
   exactly. (The command's process exit code is still `1` due to an unrelated global `fail_under =
   85` coverage-gate in `pyproject.toml` that fires on any partial-package test run — confirmed this
   is a pre-existing environmental quirk, not caused by this task, by reproducing the identical
   coverage-gate failure on the pre-fix-pass committed baseline via `git stash`, where all 178 tests
   passed with 0 failures too. The task's literal "Done when" bar (tests pass) is met at the
   test-assertion level.)

5. **`uv run pytest tests/ -q` (full suite) — NOT reported by the implementer despite an explicit
   requirement to do so.** Ran it myself: **752 passed, 2 failed** —
   `tests/engine/test_scorer.py::TestRealSampleScoring::test_real_sample_partial_count_154` and
   `::test_real_sample_manual_count_489`. These assert hardcoded confidence-distribution counts
   (154 PARTIAL, 489 MANUAL stages) against the real annotated sample; actual values are now 114 and
   480 respectively (also cross-checked `error_flag_count`: 485 expected vs. 462 actual, and
   `warn_flag_count`: 53 expected vs. 87 actual — both also drifted, though not asserted by a
   currently-failing test since the visible failures are the first two hit). **Traced this to
   pre-date the fix pass**: reverted just the `PID_0005` space-fix in a scratch copy (restoring the
   pre-fix-pass, previously-63%-reviewed catalogue state) and reran — the same 2 scorer tests still
   fail identically. This means the regression originates in the *original* Task 2b work (the Excel
   `method_patterns` rewrite, new Outlook/Screenshot/Image-Search entries, and the SharePoint
   confidence change from 0.65→0.45 all shift real annotation outcomes), not in this fix pass's own
   3 changes — but it was never caught or disclosed in either the original implementer's summary,
   my own prior 63% review (which only ran `tests/mapper/` and `tests/e2e/`), or this fix pass's
   summary, despite this round's instructions explicitly requiring the full-suite run and exact
   count to be reported. The task's own literal "Done when" bar only names `tests/mapper/ -v`, so
   this doesn't reopen the fix pass's completion status on its own terms, but it is a real,
   previously-invisible regression this review round was specifically tasked with surfacing, and
   the implementer did not do the check they were told to do.

6. **Catalogue entry count**: `yaml.safe_load` → 38 entries, confirmed. The 3 `test_config.py`
   assertions (`test_load_default_mapping_dir`, `test_load_real_vbo_catalogue`,
   `test_real_vbo_catalogue_loadable`) plus the one docstring were all updated `32` → `38`,
   correctly matching. No other counts or logic in the test file were touched.

7. **`uv run ruff check tests/mapper/test_config.py`**: `All checks passed!`

8. **Diff-against-prior-state check**: `git diff acae3f5 -- mapping/vbo_catalogue.yaml` (as
   available in this working tree) shows the changes are exactly the 3 disclosed fixes layered on
   top of the already-reviewed prior additions — Excel fusion patterns, Outlook entries, PID_0003,
   Image Search, and the Sharepoint update are byte-identical to what I reviewed previously. Clean,
   surgical, no undisclosed scope creep.

## Dimension scores

| Dimension | Score | Evidence |
|---|---|---|
| Structural | N/A | Task touches no flow/workflow generation output — pure `mapping/vbo_catalogue.yaml` data edit. |
| `FUNCTION` functional fidelity | N/A | Task touches no `FUNCTION` body generation; catalogue data remains unconsumed by the generator (unchanged from prior review). |
| VBO/action fidelity | 90% | Both defects flagged in the 63% review are now resolved and independently verified: `PID_0005` key now byte-exact-matches the real sample and live-routes correctly end-to-end (11/11 real stages now resolve at 0.15/MANUAL/UIAutomation instead of the 0.0/unknown fallback); the Screenshot template/citation now correctly cites the raw `.robin.txt` line it's actually drawn from (verified `vbo-action-mapping.md` has no Screenshot row at all, so this actually was the only correct source) and carries both real parameters. Excel (5/5 verbatim) and the Image Search / Sharepoint low-confidence "don't invent" handling remain solid as before. Not 100% only because the Outlook `External.InvokeCloudConnector`-prepended templates are still a reasonable reconstruction of the source table's `...`-continued rows rather than a literal verbatim copy — the same minor, non-fabricated gap noted in the prior review, unchanged and unaddressed by this fix pass (it wasn't in scope for the fix pass to touch). |
| Naming convention | N/A | No PAD identifiers / `In_`/`Out_` / `GLOBAL.` qualification touched. |
| Error-handling/stop/consecutive-exception | N/A | Task doesn't touch `BLOCK`/`ON BLOCK ERROR`/consecutive-exception constructs. |
| Well-formedness | 70% | YAML parses cleanly; pydantic schema loads; `tests/mapper/ -v` is now genuinely green at the test-assertion level (178/0, matching the task's literal "Done when" bar); `tests/e2e/test_pid171_pipeline.py` still 16/16. Held back from higher because the fix pass was explicitly instructed to run and report `uv run pytest tests/ -q` and did not — running it myself surfaced 2 real failures in `tests/engine/test_scorer.py` caused by this task's catalogue corrections shifting real annotation outcomes (partial/manual/error/warn counts all drifted from their previously-hardcoded values). This regression pre-dates the fix pass (traced to the original Task 2b work), so it isn't a new defect introduced by Fix 1/2/3, but the fix pass's own instructions this round required exactly this check and it was skipped — a disclosure failure, not just an inherited one. |

## Most significant gaps

1. **Undisclosed full-suite regression, despite explicit instruction to check.** `uv run pytest
   tests/ -q` was required by this fix pass's brief and not reported. Running it independently:
   752 passed, 2 failed (`test_real_sample_partial_count_154`, `test_real_sample_manual_count_489`
   in `tests/engine/test_scorer.py`), with `error_flag_count` and `warn_flag_count` also drifted
   from their hardcoded expected values (485→462, 53→87) though not yet asserted-failing. Traced to
   pre-date this fix pass (reproduced identically after reverting just the `PID_0005` space
   correction) — the root cause is the *original* Task 2b catalogue corrections (Excel
   `method_patterns` rewrite, new VBO entries, SharePoint confidence change) legitimately changing
   real annotation outcomes for the better, while a separate hardcoded-count test elsewhere in the
   suite was never updated to match. This is the same category of "hardcoded hidden test count"
   problem already found and partially fixed in `tests/mapper/test_config.py` — it just also exists,
   undiscovered, in `tests/engine/test_scorer.py`. Recommend the coordinator authorize a narrow
   follow-up (update the 4 hardcoded real-sample counts in `tests/engine/test_scorer.py` to the new
   correct values — 114/480/462/87 — after confirming those new values are in fact correct, since a
   quick sanity check here can't rule out that some individual annotation changed for the wrong
   reason) rather than treating it as blocking this task, but it must not go unrecorded a third time.
2. **Outlook templates remain a reconstruction, not a literal copy** (pre-existing, out of this
   fix pass's declared 3-item scope; not a new issue, carried forward from the 63% review
   unchanged).
3. Minor: the coverage-gate `fail_under=85` in `pyproject.toml` makes any partial-directory
   `pytest tests/mapper/` or `tests/e2e/...` invocation exit non-zero regardless of actual test
   pass/fail state — this is a pre-existing project-wide quirk unrelated to Task 2b, noted here only
   so future reviewers don't mistake the non-zero exit code for a real test failure without
   checking the "N passed" line itself.

## What's solid

- Both content defects from the 63% review are now genuinely fixed and independently verified
  end-to-end (not just string-level changes) — the `PID_0005` fix in particular was proven via a
  real annotator run against all 11 real affected stages, not just a YAML-key comparison.
- The disclosed `test_config.py` hardcoded-count conflict is correctly resolved (32→38, verified
  against the actual `yaml.safe_load` count).
- File-scope discipline remains clean — exactly the 3 assigned fixes, nothing else in
  `vbo_catalogue.yaml` disturbed.
- Ruff clean on the touched test file.

## Verdict

pass — both defects from the prior review are genuinely fixed and independently verified, and the
task's own literal "Done when" bar (`tests/mapper/ -v` passes) is met. The undiscovered full-suite
regression in `tests/engine/test_scorer.py` is real and should be tracked as a narrow follow-up
(the counts need updating to reflect the now-more-accurate annotation outcomes), but it pre-dates
this fix pass, sits outside Task 2b's declared file scope, and doesn't fail the task's own
completion criterion — it's a process/disclosure gap for a future task, not a reason to send this
fix pass back a second time.
