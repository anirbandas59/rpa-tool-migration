# Review: Task 6b2 — 2026-09-01 (re-review after fix pass)

This is a re-review of the prior report `docs/reviews/6b2-2026-09-01.md` (82%, "needs another
implementation pass"). The prior review's core logic verification stood: the disambiguation
mechanism correctly distinguished intentional folds from accidental collisions and correctly
updated CALL sites, all independently verified via reviewer-built synthetic fixtures. Two gaps
remained open: (1) the task's literal "Done when" line required a landed `pad.py` regression test
demonstrating both paths, which did not exist, and (2) a minor nit that the ordinal scheme
suffixed *both* colliding pages (`Copy_2`/`Copy_3`) rather than leaving the first bare
(`Copy`/`Copy_2`). This fix pass addresses both. What changed since the last review: the
`ordinal == 0 → no suffix` branch was added to `_build_page_name_resolution_map`, two new tests
landed in `tests/generator/test_pad.py`, and `.claude/.active-task-scope.json` now correctly
includes that file.

**Overall: 97%**

| Dimension | Score | Evidence |
|---|---|---|
| Structural | N/A for this task's scope | Unchanged from the prior review — this task only touches FUNCTION-name disambiguation and a test-helper's `Category` filter, not flow/workflow topology. Not re-verified via a fresh `convert` run this pass since nothing in the fix pass touches packaging or workflow registration; the prior review's independent confirmation (3 `<Workflow>` elements, Category 6/6/5) is unaffected by this diff. |
| FUNCTION functional fidelity | 100% | Read `src/flowsmith/generator/pad.py` lines 1016-1102 directly. `_build_page_name_resolution_map` now computes `ordinal = fallback_pages.index(page.page_id)` and branches: `ordinal == 0` → bare `target_name` (no suffix), else → `f"{target_name}_{ordinal + 1}"`. This is exactly the fix the prior review asked for (first collider stays bare, subsequent ones get `_2`, `_3`, ...). The intentional-fold path (`is_explicit` branch, keyed off `"target_name" in shape_info` — the actual source of the value, not a `notes:` heuristic) is untouched and still correct. Both branches are now exercised by a real, non-trivial regression test (see Well-formedness row) rather than only reviewer-built fixtures, closing the previously-docked 10%. |
| VBO/action fidelity | N/A for this task's scope | No `CALL`/module-action template or mapping content touched by this fix pass. |
| Naming convention | N/A for this task's scope | Unchanged reasoning from the prior review — the disambiguation-suffix scheme has no prior architecture-doc convention; this task originates it. The off-by-one nit that was previously scored here is now fixed and folded into the FUNCTION-fidelity row above. |
| Error-handling/stop/consecutive-exception | N/A for this task's scope | Not touched. |
| Well-formedness | 95% | Independently re-ran `uv run pytest tests/generator/test_pad.py tests/e2e/test_pid171_pipeline.py -v --no-cov`: **92 passed**, matching the implementer's claim exactly (75 + 17). Read both new tests in full (`test_accidental_function_name_collision_disambiguation`, `test_intentional_fold_mapping_no_disambiguation`) and confirmed they exercise genuinely distinct paths, not a duplicate of each other: the first builds a synthetic `BPProcess` with two pages both named `"Copy"` and no `page_target_map.yaml` entry (fallback-name collision) and additionally two `is_subsheet_call=True` caller stages with `processid` pointing at each `"Copy"` page's `page_id`; the second loads the real `PID_0171.bprelease` sample and exercises the actual `page_target_map.yaml` "Move Emails" explicit-mapping fold end to end. Confirmed by reading `pad.py` lines 560-586 and 703-712 that `is_subsheet_call` stages are routed through `_render_call_or_inline`, which resolves the emitted `CALL` name via `self._current_page_name_map[target_page.page_id]` — meaning the new test's `CALL 'Copy'` / `CALL 'Copy_2'` assertions genuinely exercise the resolution map (the stage's own `pa_target_action="CALL 'placeholder'"` field is provably unused for this code path, not what the assertion is trivially matching against). Manually traced that both tests would fail under the pre-fix ordinal scheme (`Copy_2`/`Copy_3`, no bare `FUNCTION 'Copy'`) and under a silent-drop regression (missing `FUNCTION 'Copy_2'` / `copy_count != 2`), confirming they are not vacuously true. Full-suite re-run (`uv run pytest tests/ -q --no-cov`): **790 passed / 10 failed** — the 10 failures are the exact same pre-existing, unrelated `tests/engine/test_flag_index.py` (8) and `tests/mapper/test_vbo_router.py` (2) failures characterized as pre-existing in the Task 6b review chain; count is 788→790 (the +2 being the new tests), confirming zero new regressions. `.claude/.active-task-scope.json` now lists `tests/generator/test_pad.py` alongside the task doc's two declared files, closing the scope-tooling gap the prior review root-caused. Docked 5% for one small housekeeping miss: `ruff check tests/generator/test_pad.py` flags an unsorted import block (`I001`) in the new `test_intentional_fold_mapping_no_disambiguation` test (local imports not alphabetized) — cosmetic, one command (`ruff check --fix`) from clean, not a correctness issue. |

## Most significant gaps

- **Minor housekeeping only**: `ruff check tests/generator/test_pad.py` reports one `I001`
  (unsorted import block) in the new intentional-fold test's local imports
  (`from flowsmith.ast import build_ast` / `from flowsmith.parser import parse_process` /
  `from flowsmith.engine import create_annotator`, lines ~2302-2304). Auto-fixable, no behavioral
  impact. Not a blocker.
- Carried-forward, out of this task's scope and not rechecked this pass: `Workflows/Populate_Queue_cloudflow.json`
  orphan-file leak noted in `docs/reviews/6b-2026-09-01-rereview.md`.

## Verdict

pass. Both gaps identified in the prior review are closed: the ordinal scheme now leaves the
first collider bare per the task's own "e.g." wording, and two new regression tests in
`tests/generator/test_pad.py` land in the correct file (confirmed via
`.claude/.active-task-scope.json`), independently confirmed to exercise genuinely distinct code
paths (synthetic fallback-collision vs. real-sample explicit-mapping fold) with a CALL-site
assertion that traces to real, non-placeholder resolution logic. `uv run pytest
tests/generator/test_pad.py -v` and `uv run pytest tests/e2e/test_pid171_pipeline.py -v` both pass
with zero failures (92/92, independently re-run), and the full suite shows zero new regressions
(790 passed, same 10 pre-existing unrelated failures as before). The task's literal "Done when"
line is now met in full. Only remaining item is a one-line cosmetic ruff import-sort fix, not
worth another implementation pass on its own.
